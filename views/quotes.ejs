<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Quotes</title>
    <link rel="stylesheet" href="/main.css">
</head>
<body class="animated-bg">
    <div class="quotes">
        <div class="quotes-header">
            <a href="/" class="home-button">🏠</a> 
            <div class="quotes-title">All Quotes</div>
        </div>

        <% quotes.forEach(quote => { %>
            <div class="quote">
                <div class="quote-actions">
                    <form action="/delete-quote/<%= quote.id %>" method="POST" style="display: inline;">
                        <input type="hidden" name="_method" value="POST"> <!-- Ensure this matches the override setup -->
                        <button class="delete-btn" title="Delete"> 🗑️ </button>
                    </form>
                    <a href="/edit-quote">
                        <button class="edit-btn" title="Edit">✏️</button>
                    </a>
                </div>
                <blockquote><%- quote.text %></blockquote>
                <div class="quote-footer">
                    <span class="quote-author"><%= quote.author %></span>,
                    <span class="quote-date"><%= quote.date %></span>
                </div>
            </div>
        <% }) %>

        <a href="/add-quote" class="button-link">
            <button class="add-quote-btn" title="Додати нову цитату">➕</button>
          </a>
    </div>
</body>
</html>
